#include "main.h"

int clamp(int num, int lower, int upper);

double dclamp(double num, double lower, double upper);

int normalize(int num, int multiple, bool pos = true);

double dnormalize(double num, double multiple, bool pos = true);